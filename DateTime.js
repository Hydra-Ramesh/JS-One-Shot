const now = new Date();
console.log(now);

console.log(now.getFullYear()); // returns the current year
console.log(now.getMonth()+1); // returns the current month (0-11, so we add 1)
console.log(now.getDate());
console.log(now.getDay());